name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.3.1'
      SERVERPOD_PASSWORD_database: 'lAKX1Dzcgc6mDbX6xmGdis99msk-s3wq'
      SERVERPOD_PASSWORD_redis: 'gFKQkW6FYxvaLF0SkIiIlM3PJXBtXkZk'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: translation_study_app_server
        run: docker compose up --build -d

      - name: Install dependencies
        working-directory: translation_study_app_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: translation_study_app_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: translation_study_app_server
        run: serverpod generate

      - name: Run tests
        working-directory: translation_study_app_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: translation_study_app_server
        run: docker compose down -v
